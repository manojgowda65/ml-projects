import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-rating-predictor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
